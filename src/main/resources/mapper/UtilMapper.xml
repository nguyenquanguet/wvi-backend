<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.vnmo.backend.mapper.UtilMapper">

    <select id="findUtil" resultType="com.vnmo.backend.domains.Util">
        SELECT * FROM util ORDER BY id DESC LIMIT 1
    </select>
</mapper>